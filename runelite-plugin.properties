displayName=OSPulse
author=jonathanavis96
description=Tracks accurate session profit (banking-aware, so bank trips are not counted as losses), a complete loot feed, total net worth (bank + inventory + equipment + GE), XP gained, and GE flip P&L. All valuation uses RuneLite's built-in GE prices. Includes an optional, OFF-by-default sync that, only when you enable it and supply a URL + token, sends your session and wealth summary to a companion dashboard you configure.
tags=profit,loot,wealth,gp,session,tracker,ge,flipping,xp,combat,dps,gear
plugins=com.ospulse.OSPulsePlugin
version=0.1.0
build=standard
